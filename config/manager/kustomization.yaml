resources:
- manager.yaml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
- files:
  - controller_manager_config.yaml
  name: manager-config
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
images:
- name: controller
  newName: harbor.sc2vc03.cmbu.local/smcgeown/vra-crd
  newTag: 0.0.4

secretGenerator:
- files:
  - refreshToken
  name: auth-secret
